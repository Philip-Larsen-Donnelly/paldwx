# Module: graphite
# Docs: https://www.elastic.co/guide/en/beats/metricbeat/6.5/metricbeat-module-graphite.html

{% if module_configuration is defined and (module_configuration | selectattr("module", "match", "graphite") | list | length)  %}
{# Module custom settings #}
{{ module_configuration | selectattr("module", "match", "graphite") | list | to_nice_yaml }}
{% else %}
{# Module default settings #}
- module: graphite
  #metricsets:
  #  - server
  #protocol: "udp"
  #templates:
  #  - filter: "test.*.bash.*" # This would match metrics like test.localhost.bash.stats
  #    namespace: "test"
  #    template: ".host.shell.metric*" # test.localhost.bash.stats would become metric=stats and tags host=localhost,shell=bash
  #    delimiter: "_"
{% endif %}
