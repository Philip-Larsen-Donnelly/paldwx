# Module: aerospike
# Docs: https://www.elastic.co/guide/en/beats/metricbeat/6.5/metricbeat-module-aerospike.html

{% if module_configuration is defined and (module_configuration | selectattr("module", "match", "aerospike") | list | length)  %}
{# Module custom settings #}
{{ module_configuration | selectattr("module", "match", "aerospike") | list | to_nice_yaml }}
{% else %}
{# Module default settings #}
- module: aerospike
  #metricsets:
  #  - namespace
  period: 10s
  hosts: ["localhost:3000"]
{% endif %}
